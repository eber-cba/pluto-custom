---
import Layout from "../layouts/Layout.astro";
import MainApp from "../components/react/MainApp.jsx";
import { fetchProducts } from "../utils/api.js";

const products = await fetchProducts();
---

<Layout>
  <MainApp products={products} client:load />
</Layout>
